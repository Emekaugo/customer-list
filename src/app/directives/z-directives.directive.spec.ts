import { ZDirectivesDirective } from './z-directives.directive';

describe('ZDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new ZDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
